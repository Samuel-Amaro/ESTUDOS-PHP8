<?php

/**
 * * TRAITS(TRAÇOS)
 * 
 * O PHP implementa uma maneira de reutilizar o código chamado Traits.
 * 
 * Traits são um mecanismo para reutilização de código em linguagens de herança única(so pode herdar de um super classe direta), como PHP. Um Trait tem como objetivo reduzir algumas limitações de herança única, permitindo que um desenvolvedor reutilize conjuntos de métodos livremente em várias classes independentes que vivem em diferentes hierarquias de classes. A semântica da combinação de Traits e classes é definida de uma forma que reduz a complexidade e evita os problemas típicos associados a herança múltipla e Mixins.
 * 
 * Uma Característica é semelhante a uma classe, mas destina-se apenas a agrupar a funcionalidade de maneira refinada e consistente. Não é possível instanciar uma Característica por conta própria. É um acréscimo à herança tradicional e permite a composição horizontal do comportamento; ou seja, a aplicação de membros da classe sem exigir herança.
 * 
 * 
 */

?>