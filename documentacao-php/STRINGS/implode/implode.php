<?php 

/**
 * * IMPLODE(implodir)
 * 
 * implode — Junta elementos de uma array em uma string
 * 
 * * DESCRIÇÃO
 
    implode(string $separator, array $array): string

    Assinatura alternativa (não compatível com argumentos nomeados):

        implode(array $array): string

 *
 * Junte os elementos da array com uma separator string.
 * 
 * Retorna uma string contendo os elementos da array na mesma ordem com uma ligação entre cada elemento.
 * 
 * * PARÂMETROS
 * 
 * separator(separador): Opcional. O padrão é uma string vazia.
 * 
 * array: O conjunto de strings para implodir.
 * 
 * * VALORES RETORNADOS
 * 
 * Retorna uma string contendo uma representação de string de todos os elementos da array na mesma ordem, com a string separadora entre cada elemento.
 * 
 * * NOTAS
 * 
 * Nota : esta função é segura para binários.
*/


?>